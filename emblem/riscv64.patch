--- PKGBUILD
+++ PKGBUILD
@@ -47,7 +47,8 @@
 }
 
 check() {
-  meson test -C build --print-errorlogs --no-rebuild
+  # Cargo tests share a build directory and compile slowly on RISC-V.
+  meson test -C build --print-errorlogs --no-rebuild --num-processes 1 --timeout-multiplier 0
 }
 
 package() {
