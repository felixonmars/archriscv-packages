--- PKGBUILD
+++ PKGBUILD
@@ -121,7 +121,7 @@
 
 if (( _manual_clone )); then
   source[0]=fetch-chromium-release
-  sha256sums[0]='2e2f36e3cd1ebc4ad57fd310774a5e5e9db77883d5f9374fedeaabd3c103b819'
+  sha256sums[0]='8c813ca28588154d0121c97a47ec1ae756765e9d6b739be1cd67fe42ef14ed61'
   makedepends+=('python-httplib2' 'python-pyparsing' 'python-six' 'npm' 'rsync')
 fi
 
@@ -191,6 +191,12 @@
   # runtime -- this allows signing into Chromium without baked-in values
   patch -Np1 -i ../use-oauth2-client-switches-as-default.patch
 
+  # RISC-V
+  patch -Np0 -i ../swiftshader-use-llvm16.patch
+  patch -Np1 -i ../riscv-sandbox.patch
+  patch -Np0 -i ../Debian-fix-rust-linking.patch
+  patch -Np1 -i ../dawn-add-riscv64-cipd-platform.patch
+
   # Upstream fixes
 
   # Fixes from Gentoo
@@ -247,13 +253,14 @@
   mkdir -p third_party/node/linux/node-linux-x64/bin \
            third_party/jdk/current/bin \
            third_party/rust-toolchain/bin \
-           third_party/dawn/tools/golang/linux-amd64/bin
+           third_party/gperf/cipd/bin \
+           third_party/dawn/tools/golang/linux-riscv64/bin
 
   ln -s /usr/bin/node third_party/node/linux/node-linux-x64/bin/
   ln -s /usr/bin/java third_party/jdk/current/bin/
+  ln -s /usr/bin/go third_party/dawn/tools/golang/linux-riscv64/bin/
   if (( !_manual_clone )); then
     ln -s /usr/bin/rustc third_party/rust-toolchain/bin/
-    ln -s /usr/bin/go third_party/dawn/tools/golang/linux-amd64/bin/
   fi
 
   # remove x86_64 binary and use our own
@@ -475,4 +482,15 @@
   install -Dvm644 LICENSE "$pkgdir/usr/share/licenses/chromium/LICENSE"
 }
 
+source+=(swiftshader-use-llvm16.patch
+         riscv-sandbox.patch
+         Debian-fix-rust-linking.patch
+         DEPS-remove-native-binaries.diff
+         dawn-add-riscv64-cipd-platform.patch)
+sha256sums+=('2ea949ed1d20a1745ce72f760a7d9297dc0002a747c4bd53e243c4d58ba2c7ca'
+             '7b5e90a85835ed867103a9e3bf7b334d35a3ac50dab58b7642978f6ec5fb9821'
+             '41deb67bbffe54dab32415693c3db56b0cc532aecd0481dba7f2abb48e01bec3'
+             '1c4f34675ed826bff5eff7ee8a3d3004c2a7842072b931526f26debd040b96bb'
+             'c4b8214f01b53cab07c8bdbb9eb2ee73493b677da531c9cb7badc51ac6edfd7f')
+
 # vim:set ts=2 sw=2 et:
--- fetch-chromium-release
+++ fetch-chromium-release
@@ -24,11 +24,42 @@
 
 git clone --depth=1 https://chromium.googlesource.com/chromium/tools/depot_tools
 export PATH+=":$PWD/depot_tools" DEPOT_TOOLS_UPDATE=0
-# system python fails to load six.moves module
-#export VPYTHON_BYPASS='manually managed python not supported by chrome operations'
+
+# gsutil needs Python 3.11, but depot_tools' bundled wheels are unavailable on
+# riscv64. Use a local environment for both legacy and TOML vpython configs.
+printf 'python_version: "3.11"\n' > rv.vpython3
+vpython3 -vpython-spec rv.vpython3 -m venv rv-venv
+source rv-venv/bin/activate
+python -m pip install httplib2==0.13.1 six==1.10.0 requests==2.31.0 crcmod==1.7
+export VPYTHON_BYPASS='manually managed python not supported by chrome operations'
+
+# Sync often fails on SG2042 with:
+# lockfile.LockError: Error locking /build/chromium/src/chromium-checkout/depot_tools/external_bin/gsutil/gsutil_5.35 (err: [Errno 11] Resource temporarily unavailable)
+retry() {
+  local retries=$1
+  shift
+
+  local count=0
+  until "$@"; do
+    exit=$?
+    wait=$((2 ** $count))
+    count=$(($count + 1))
+    if [ $count -lt $retries ]; then
+      echo "Retry $count/$retries exited $exit, retrying in $wait seconds..."
+      sleep $wait
+    else
+      echo "Retry $count/$retries exited $exit, no more retries left."
+      return $exit
+    fi
+  done
+  return 0
+}
+
 
 git clone -b $VERSION --depth=2 https://chromium.googlesource.com/chromium/src
-gclient sync --no-history --nohooks
+patch -Np1 -d src < ../DEPS-remove-native-binaries.diff
+retry 5 gclient sync --no-history --nohooks
+
 src/build/util/lastchange.py -o src/build/util/LASTCHANGE
 src/build/util/lastchange.py -m GPU_LISTS_VERSION \
   --revision-id-only --header src/gpu/config/gpu_lists_version.h
@@ -43,5 +74,6 @@
 src/third_party/node/update_npm_deps
 
 rm -rf src/third_party/node/linux/node-linux-x64/bin/
+mkdir -p src/third_party/jdk/current/bin/
 find src/third_party/jdk/current -type f -delete
 mv src ../chromium-$VERSION
