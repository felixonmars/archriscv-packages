--- a/src/lib.rs
+++ b/src/lib.rs
@@ -56,6 +56,11 @@
 #[path = "impl-x86_64-win64.rs"]
 mod imp;
 
+// riscv64+unix uses the same 64-bit slots as x86_64+windows.
+#[cfg(all(target_arch = "riscv64", target_family = "unix"))]
+#[path = "impl-x86_64-win64.rs"]
+mod imp;
+
 // x86+unix = cdecl
 #[cfg(all(target_arch = "x86", target_family = "unix"))]
 #[path = "impl-x86-sysv.rs"]
