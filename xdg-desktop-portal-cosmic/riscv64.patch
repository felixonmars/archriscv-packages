--- PKGBUILD
+++ PKGBUILD
@@ -42,7 +42,7 @@ pkgver() {
 
 build() {
   cd xdg-desktop-portal-cosmic
-  RUSTFLAGS+=" -C link-arg=-fuse-ld=lld"
+  #RUSTFLAGS+=" -C link-arg=-fuse-ld=lld"
   make ARGS+=" --frozen --release"
 }
 
