--- PKGBUILD
+++ PKGBUILD
@@ -17,6 +17,8 @@
 prepare() {
   cd "$pkgname-$pkgver"
 
+  # Backport upstream d4bb6e0 for cfg! type-checking TEE-only memory attributes on riscv64.
+  patch -Np1 -i ../fix-riscv64-non-tee-memory-region-setup.patch
   cargo fetch --locked --target "$(rustc --print host-tuple)"
 }
 
@@ -34,3 +36,6 @@
 
   install -Dm644 LICENSE "$pkgdir"/usr/share/licenses/$pkgname/LICENSE
 }
+
+source+=("fix-riscv64-non-tee-memory-region-setup.patch")
+sha256sums+=('6a3bde92bd2ae6d518907a68b1c659126db309badec4e945aceb31bfa242a752')
