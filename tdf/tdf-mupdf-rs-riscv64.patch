--- a/mupdf-sys/make.rs
+++ b/mupdf-sys/make.rs
@@ -196,7 +196,8 @@
         self.make_bool("HAVE_GLUT", false);
         self.make_bool("HAVE_CURL", false);
 
-        if target.arch == "wasm32" {
+        // GNU ld -b binary omits the hard-float ELF flag on RISC-V.
+        if target.arch == "wasm32" || target.arch == "riscv64" {
             self.make_bool("HAVE_OBJCOPY", false);
         }
 
