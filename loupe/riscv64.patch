--- PKGBUILD
+++ PKGBUILD
@@ -49,6 +49,9 @@
 prepare() {
   cd $pkgname
 
+  # Keep cargo-test in the same build environment as cargo-build.
+  git cherry-pick -n e896816db60a21aa30f3dec280f69f91be0045e0
+
   # Match CARGO_HOME in src/meson.build
   CARGO_HOME="$srcdir/build/cargo-home" \
     cargo fetch --locked --target host-tuple
@@ -60,7 +63,7 @@
 }
 
 check() {
-  meson test -C build --print-errorlogs --no-rebuild
+  meson test -C build --print-errorlogs --no-rebuild --timeout-multiplier 2
 }
 
 package() {
