diff --git PKGBUILD PKGBUILD
index 03f5884..9f28bc9 100644
--- PKGBUILD
+++ PKGBUILD
@@ -25,6 +25,8 @@ prepare() {
   patch -Np1 -i "$srcdir"/openjade-nola.patch
   # Patch from Fedora
   patch -Np1 -i "$srcdir"/openjade-getoptperl.patch
+  # Fix `unable to guess system type` on riscv64
+  cp -v /usr/share/autoconf/build-aux/config.{guess,sub} config
 }
 
 build() {
