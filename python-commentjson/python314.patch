--- a/commentjson/commentjson.py
+++ b/commentjson/commentjson.py
@@ -31,6 +31,9 @@
 from lark.tree import Tree
 
 
+# common.ESCAPED_STRING does not match unescaped newlines, so json.loads
+# never sees them to apply strict=. Allow every character except an
+# unescaped quote inside strings and leave JSON string validation to json.
 parser = Lark('''
     ?start: value
     ?value: object
@@ -44,11 +47,11 @@
     object : "{" [pair ("," pair)*]  TRAILING_COMMA? "}"
     pair   : string ":" value
     string : ESCAPED_STRING
+    ESCAPED_STRING: /"(?:[^"\\\\]|\\\\(?:.|\\n))*"/
 
     COMMENT: /(#|\\/\\/)[^\\n]*/
     TRAILING_COMMA: ","
 
-    %import common.ESCAPED_STRING
     %import common.SIGNED_NUMBER
     %import common.WS
     %ignore WS
@@ -175,6 +178,10 @@
 
     if isinstance(text, (bytes, bytearray)):
         text = text.decode(detect_encoding(text), 'surrogatepass')
+    elif not isinstance(text, (str, type(u''))):
+        raise TypeError(
+            'the JSON object must be str, bytes or bytearray, '
+            'not %s' % (text.__class__.__name__,))
 
     try:
         parsed = _remove_trailing_commas(parser.parse(text))
--- a/commentjson/tests/test_json/__init__.py
+++ b/commentjson/tests/test_json/__init__.py
@@ -7,3 +7,5 @@
     json = commentjson
     loads = staticmethod(commentjson.loads)
     dumps = staticmethod(commentjson.dumps)
+    # stdlib json tests use self.JSONDecodeError; commentjson raises ValueError
+    JSONDecodeError = ValueError
--- a/commentjson/tests/test_json/test_decode.py
+++ b/commentjson/tests/test_json/test_decode.py
@@ -74,3 +74,8 @@
                      'Infinity as a value is not supported yet')
     def test_parse_constant(self):
         pass
+
+    @unittest.skipIf(version >= (3, 14),
+                     'JSONDecoder.raw_decode is not supported by commentjson')
+    def test_unterminated_string(self):
+        pass
