diff --git PKGBUILD PKGBUILD
index 24e6af2..c6ffeec 100644
--- PKGBUILD
+++ PKGBUILD
@@ -20,6 +20,9 @@
 
 build() {
   cd $pkgname
+  # clang does not support -fno-plt on riscv64
+  CFLAGS="${CFLAGS//-fno-plt/}"
+  CXXFLAGS="${CXXFLAGS//-fno-plt/}"
   ./build/gen.py
   ninja -C out
 }
