--- PKGBUILD
+++ PKGBUILD
@@ -23,7 +23,7 @@ prepare() {
   cd "$_pkgname-$pkgver"
 
   # download dependencies
-  cargo fetch --locked --target "$CARCH-unknown-linux-gnu"
+  cargo fetch --locked
 }
 
 build() {
