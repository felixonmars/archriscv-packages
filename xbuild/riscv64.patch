--- PKGBUILD
+++ PKGBUILD
@@ -32,6 +32,8 @@ sha256sums=('f006de3e08cc8a6bb7c67df83b28d63dddb38a4fcd31565f294b7a51b777c82e')
 
 prepare() {
   cd "${pkgname}-${pkgver}"
+  echo -e "\n[patch.crates-io]\nring = { git = 'https://github.com/felixonmars/ring', branch = '0.16.20' }" >> Cargo.toml
+  cargo update -p ring
   cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
 }
 
