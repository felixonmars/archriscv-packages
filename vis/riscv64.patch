--- PKGBUILD
+++ PKGBUILD
@@ -53,6 +53,9 @@
   done
 
   sed -i '/^.PHONY/d' Makefile
+
+  # Let slow builders consume Escape before the next key becomes an Alt chord.
+  sed -i 's/usleep(100\*1000)/usleep(500*1000)/' test/util/keys.c
 }
 
 build() {
