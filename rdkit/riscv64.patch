diff --git PKGBUILD PKGBUILD
index 0bda504..e8a1913 100644
--- PKGBUILD
+++ PKGBUILD
@@ -40,6 +40,7 @@
     -DRDK_INSTALL_STATIC_LIBS=OFF
     # TODO: the test suite has too many failures, even due to bad input files
     -DRDK_BUILD_CPP_TESTS=OFF
+    -DRDK_OPTIMIZE_POPCNT=OFF
   )
   cmake "${cmake_options[@]}"
   cmake --build build
