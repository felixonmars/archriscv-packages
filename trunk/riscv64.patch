--- PKGBUILD
+++ PKGBUILD
@@ -32,7 +32,9 @@
 
 check() {
   cd "$pkgname-$pkgver"
-  cargo test --release --no-default-features --features native-tls
+  # The binary download test only supports x86_64 and aarch64.
+  cargo test --release --no-default-features --features native-tls -- \
+    --skip tools::tests::download_and_install_binaries
 }
 
 package() {
