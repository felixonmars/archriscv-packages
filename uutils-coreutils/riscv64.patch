--- PKGBUILD
+++ PKGBUILD
@@ -47,8 +47,10 @@
 
 check() {
   cd $pkgname
+  # Avoid the du test's process-wide chdir race and timing-test contention.
   make test \
     TEST_NO_FAIL_FAST="-- \
+    --test-threads=1 \
     --skip test_chgrp::test_from_with_invalid_group \
     --skip test_chmod::test_chmod_colored_output \
     --skip test_env::test_env_french \
