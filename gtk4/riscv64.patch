--- PKGBUILD
+++ PKGBUILD
@@ -96,6 +96,8 @@
 
 build() {
   local meson_options=(
+    # Concurrent links of the static GTK libraries exhaust memory with LTO.
+    -D backend_max_links=2
     -D broadway-backend=true
     -D cloudproviders=enabled
     -D colord=enabled
