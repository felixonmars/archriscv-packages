diff --git PKGBUILD PKGBUILD
index 568d2ac..b7d705f 100644
--- PKGBUILD
+++ PKGBUILD
@@ -37,6 +37,7 @@ build() {
   cd haproxy-$pkgver
 
   export CFLAGS="$CFLAGS -fwrapv"
+  export LDFLAGS="$LDFLAGS -latomic"
 
   make \
     CFLAGS="$CFLAGS" \
