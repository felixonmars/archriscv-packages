--- PKGBUILD
+++ PKGBUILD
@@ -18,7 +18,7 @@ prepare() {
   cd "$pkgname-$pkgver"
 
   # download dependencies
-  cargo fetch --locked --target "$CARCH-unknown-linux-gnu"
+  cargo fetch --locked
 }
 
 build() {
