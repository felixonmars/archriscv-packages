diff --git PKGBUILD PKGBUILD
index 35d60838..0e2c9e49 100644
--- PKGBUILD
+++ PKGBUILD
@@ -53,6 +53,7 @@ prepare() {
 
 build(){
   cd "${pkgbase}-${pkgver}"
+  find . -name config.guess -exec cp -f /usr/share/libtool/build-aux/config.guess {} \;
   ./configure --prefix=/usr --without-gimp
   make
 }
