--- PKGBUILD
+++ PKGBUILD
@@ -63,3 +63,9 @@ package() {
   DESTDIR="$pkgdir" cmake --install build
   install -vDm644 -t "$pkgdir/usr/share/doc/$pkgname" ./*.md
 }
+
+options=(!lto)
+checkdepends+=(
+  'unrar'
+  '7zip'
+)
