--- PKGBUILD
+++ PKGBUILD
@@ -47,6 +47,10 @@
 
 prepare() {
   cd at-spi2-core
+
+  # Other AT-SPI tests can open direct connections and change the pidfd count.
+  sed -i "/^test('direct-connection',/s/timeout: 30/timeout: 30, is_parallel: false/" \
+    tests/at-spi2-atk/meson.build
 }
 
 build() {
